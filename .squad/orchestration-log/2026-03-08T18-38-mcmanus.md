# McManus — LLM Auto-Auth Implementation
**Date:** 2026-03-08T18-38 | **Agent:** claude-sonnet-4.5

## Summary
Implemented server-side LLM authentication via GitHub CLI proxy. Auth tokens now handled securely without frontend exposure.

## Tasks Completed

### 1. Vite Config Proxy Auto-Auth
- Updated `vite.config.ts` to inject `Authorization` header via `gh auth token`
- Added `/api/auth/status` endpoint for frontend auth checks
- `gh` CLI token fetched on each request (fresh tokens, automatic rotation)

### 2. Type System Updates
- Removed `apiKey` from `LLMConfig` interface in `src/types.ts`
- Eliminated need for API key storage in browser

### 3. LLMPlayer Updates
- `src/llm-player.ts`: Added static `checkAuth()` method
- Removed manual auth header injection (proxy now handles it)
- Frontend sends zero credentials

## Impact
- Zero-friction: Users with `gh auth login` get automatic LLM mode
- Security: No auth tokens in browser or localStorage
- Freshness: Rotated tokens picked up automatically on each request

## Files Modified
- `vite.config.ts` — proxy auth + status endpoint
- `src/types.ts` — removed `apiKey` from `LLMConfig`
- `src/llm-player.ts` — auth checking, removed auth headers

## Status
✅ Implementation complete. Commit: 80a9c30
