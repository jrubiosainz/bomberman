# Orchestration Log: Keaton

**Timestamp:** 2025-01-XX

## Assignment

Design Hollow Depths architecture — types.ts, constants.ts, level1.ts, input.ts, main.ts. Set up new project at hollow-depths/.

## Outcome

✅ **COMPLETE**

- **types.ts** (400+ lines) — Comprehensive type system covering all game entities, systems, and state
- **constants.ts** — All tuning parameters (physics, player stats, weapon data, enemy data, boss stats, XP curve)
- **level1.ts** (160×50 map) — Complete level design with enemy spawns, NPC dialogue, weapon pickups, boss arena
- **input.ts** — Input handler distinguishing held keys (movement) from pressed keys (actions)
- **main.ts** — Fixed timestep game loop with 1280×720 canvas, initialization, bootstrap

## Technical Details

- Pure functional architecture: `update(state, actions, dt) → GameState`
- TypeScript strict mode enabled, ES2020 target, Vite bundler
- No mutations in update path — all entities copied via spread operators
- Entity system: plain data objects (no classes), fully typed interfaces
- Level structure: tile-based metroidvania with 160×50 tiles, 32px each

## Dependencies Installed

✅ npm install — 104 packages  
✅ TypeScript compiles — no errors  
✅ Git initialized and committed  

## Verification

```bash
npx tsc --noEmit  # Clean
npm run dev       # Runs on localhost:3000
```

Placeholder game loop displays architecture ready message. McManus and Fenster ready to integrate game logic and renderer.

## Integration Points

- **McManus:** Replace `update()` function with game engine (physics, AI, combat, particles, progression)
- **Fenster:** Replace `render()` function with visual system (tiles, entities, particles, glow effects, UI)
- Main loop wiring complete; minimal merge conflicts expected

## Key Decisions Documented

1. Pure functional game state (testability, debuggability, determinism)
2. Comprehensive types-first approach (parallel development enablement)
3. Constants as configuration (game feel tuning separation)
4. Tile-based metroidvania structure (familiar, scalable, level-editor-ready)
5. Input distinction (held vs pressed prevents action repeats)
6. Fixed timestep game loop (deterministic physics)
7. Plain data entities (serializable, testable, no hidden state)
8. Team separation by concern (logic / render / input)

## Status

Ready for game engine and renderer integration. Architecture validated. All files committed.
